rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Product images can be read by anyone, but only uploaded by authenticated users.
    match /product-images/{imageId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all product images
    // and only allow authenticated users to write (upload) new images.
    match /product-images/{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
